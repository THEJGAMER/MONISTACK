[pytest]
; webui/*.py are flat top-level modules (app.py, parsers.py, ...), not a
; package - this lets tests `import parsers`/`import app` etc. the same
; way the app itself does, regardless of where pytest is invoked from.
pythonpath = .
testpaths = tests
